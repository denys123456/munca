spring:
  datasource:
    url: jdbc:h2:mem:championsclub;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

championsclub:
  ml:
    base-url: http://localhost:8000
  ai:
    provider: MOCK
  security:
    demo-password: test-password

